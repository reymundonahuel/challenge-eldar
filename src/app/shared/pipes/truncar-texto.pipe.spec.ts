import { TruncarTextoPipe } from './truncar-texto.pipe';

describe('TruncarTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncarTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
